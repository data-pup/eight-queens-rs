use {Queens, UncontestedSpaces};
use board::Board;

pub struct Solver {
    board: Board,
    is_solved: bool,
}

impl Solver {
    fn new(b: Board) {
        unimplemented!();
    }

    fn check_is_solved(b: Board) -> bool {
        unimplemented!();
    }
}
