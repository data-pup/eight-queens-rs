use {Position, Queens};
use super::Board;

impl Queens for Board {
    fn add_queen(&mut self, row: u32, col: u32) {
        unimplemented!();
    }

    fn get_queen_positions(&self) -> Vec<Position> {
        unimplemented!();
    }
}
