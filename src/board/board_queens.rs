use Queens;
use position_types::*;
use super::Board;

impl Queens for Board {
    fn add_queen(&mut self, row: u32, col: u32) {
        unimplemented!();
    }

    fn get_queen_positions(&self) -> Vec<PosCoords> {
        unimplemented!();
    }
}
